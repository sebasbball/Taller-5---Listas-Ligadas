using Taller5ListasLigadas.UI;

namespace Taller5ListasLigadas
{
    // Main entry point of the application
    class Program
    {
        static void Main()
        {
            // Create and start the menu
            Menu menu = new Menu();
            menu.Start();
        }
    }
}